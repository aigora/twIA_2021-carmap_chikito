#include <stdio.h>
#include <stdlib.h>
/*#include "Keyboard.h"
#include "robot.h"
#include "vector.h"*/

enum perfiles {
	Perfil1,
	Perfil2
};

int main(void) {
	//configuración (setup);
	//bucle principal
	//cierre
	;
}