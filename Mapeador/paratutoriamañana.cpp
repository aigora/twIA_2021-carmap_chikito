#include <iostream>
#include <chrono>

typedef struct tiempo
{ 
	float n, s, e, o;//direciones como si fuese una brujula

};

typedef struct numeroDePulsaciones
{
	float np, ns, es, os;

};


int main(void)
{
	auto start::std::chrono::steady_clock::now();
	std::cout << tiempo.n, tiempo.s, tiempo.e, tiempo.o(); << std end;
    auto end::std::chrono::steady_clock::now();
	std::chrono::duration<float> duration = end - start;


}




















